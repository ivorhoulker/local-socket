/* eslint-disable @typescript-eslint/no-var-requires */
const path = require("path");
require("ts-node").register();
require(path.resolve(__dirname, "./wheelWorker.ts"));
require(path.resolve(__dirname, workerData.path));
